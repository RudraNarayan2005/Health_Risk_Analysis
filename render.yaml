services:
  - type: web
    name: health-risk-analysis
    env: python
    buildCommand: bash build.sh
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
